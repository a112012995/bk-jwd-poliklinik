@if ($type === 'none')
    <nav>

    </nav>
@elseif ($type === 'guest')
    <nav>

    </nav>
@elseif ($type === 'user')
    <nav>

    </nav>
@elseif ($type === 'admin')
    <nav>

    </nav>
@endif
